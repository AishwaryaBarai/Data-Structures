def matchingStrings(strings, queries):
    result = []
    for q in queries:
        count = 0
        for s in strings:
            if str(q) == str(s):
                count +=1
        result.append(count)
    return (result)
