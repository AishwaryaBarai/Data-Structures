def matchingStrings(strings, queries):
    result = []
    for q in queries:
        cnt = 0
        for s in strings:
            if str(q) == str(s):
                cnt +=1
        result.append(cnt)
    return (result)
