def checkMagazine(magazine, note):
    c1 = collections.Counter(magazine)
    c2 = collections.Counter(note)
    for i in c2:
        if c2[i] > c1[i]:
            print ("No")
            return
    print('Yes')
